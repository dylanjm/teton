[user]
	name = Dylan McDowell
	email = dylanjm@users.noreply.github.com
[core]
    pager = diff-so-fancy | less --tabs=4 -RFX
    warnAmbiguousRefs = true
	editor = emacsclient -a "emacs" -c
	whitespace = fix,-indent-with-non-tab,trailing-space,cr-at-eol
	excludesfile = /Users/mcdodj/.gitignore
[github]
    user = dylanjm
[rebase]
    autosquash = true
[push]
    default = current
[branch]
    autosetuprebase = always
[color]
	ui = auto
[color "branch"]
       current = yellow bold
       local = green bold
       remote = cyan bold
[color "diff"]
       meta = yellow bold
       frag = magenta bold
       old = red bold
       new = green bold
       whitespace = red reverse
[color "status"]
       added = green bold
       changed = yellow bold
       untracked = red bold
