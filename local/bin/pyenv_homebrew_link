#!/usr/local/bin/zsh

for i in `ls $(brew --cellar python)/`; do
  ln -s $(brew --cellar python)/$i $HOME/.config/pyenv/versions/$i-brew;
done

for i in `ls $(brew --cellar python@3.8)/`; do
  ln -s $(brew --cellar python@3.8)/$i $HOME/.config/pyenv/versions/$i-brew;
done
