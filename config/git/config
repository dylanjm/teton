# -*- mode: gitconfig; -*-
[user]
  name = Dylan McDowell
  email = dylanjm@users.noreply.github.com
[core]
  pager = delta
  whitespace = fix,-indent-with-non-tab,trailing-space,cr-at-eol
  excludesfile = ~/.config/git/ignore
  editor = /usr/local/bin/emacsclient -a '' -t -c
[github]
  user = dylanjm
[gitlab]
  user = mcdodyla
  email = dylan.mcdowell@inl.gov
[rebase]
  autoSquash = true
[push]
  default = simple
[branch]
  autoSetupRebase = always
[color]
  ui = auto
  advice = auto
[color "branch"]
  current = yellow bold
  local = green bold
  remote = blue bold
[color "diff"]
  meta = yellow bold
  frag = magenta
  old = red
  new = green bold
  whitespace = red reverse
[color "status"]
  added = green bold
  changed = yellow bold
  untracked = red bold
[color "grep"]
  filename = magenta bold
  lineNumber = green bold
  match = red bold
[column]
  ui = auto
# [interactive]
#   diffFilter = delta --color-only
# [delta]
#   features = side-by-side line-numbers decorations
#   whitespace-error-style = 22 reverse
# [delta "decorations"]
#   commit-decoration-style = bold yellow box ul
#   file-style = bold yellow ul
#   file-decoration-style = none
