name: CI

on: [push]

jobs:
  build:
    runs-on: [macos-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Tmpfile Creation
        run: echo -e "2\n4" > tmpfile
      - name: Setup
        run: ./install < tmpfile
