name: CI

on: [push]

jobs:
  build:
    runs-on: [macos-latest]

    steps:
      - uses: actions/checkout@v1
      - name: Bootstrap Tools
        run: |
          rm /usr/local/bin/node
          bash ./install < test/bootstrap-tools.txt
