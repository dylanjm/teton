export SLIMLINE_ENABLE_ASYNC_AUTOLOAD=0;
export SLIMLINE_SYMBOL_WORKING_FORMAT="%F{red}❯%f"
export SLIMLINE_SYMBOL_READY_FORMAT="%F{magenta}❯%f"

export GITLINE_REPO_INDICATOR="${reset}"
export GITLINE_BRANCH='[${green}${branch}${reset}]'

export GITLINE_UNSTAGED_MODIFIED='${unstaged_modified}${yellow}M${reset}'
export GITLINE_UNSTAGED_DELETED='${unstaged_modified}${yellow}D${reset}'
export GITLINE_UNTRACKED='${untracked}${red}?${reset}'
