name: CI

on: [push]

jobs:
  build:
    runs-on: [macos-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup
        run: ./install < test/bootstrap-tools.txt
